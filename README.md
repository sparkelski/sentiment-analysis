## Sentiment Analysis

Used C, Makefile, and the Ubuntu Bash unix shell. The program implements dynamic memory management using malloc and realloc to store lexical entries. The program also tokenizes input strings by removing delimiters and managing case sensitivity. The LADER lexicon and test script are provided here.
